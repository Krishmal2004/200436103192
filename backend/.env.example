# Server Configuration
SERVER_PORT=8080

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=Task01
DB_USERNAME=postgres
DB_PASSWORD=<add your password here>

# JWT Configuration
# Generate a strong 256-bit secret key (e.g., using openssl rand -base64 32)
JWT_SECRET=<add your jwt secret here>
# Expiration time in milliseconds (e.g., 86400000 = 24 hours)
JWT_EXPIRATION=86400000
